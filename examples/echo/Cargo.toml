[package]
name = "echo"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes = { version = "1" }
tokio = { version = "1", features = ["rt", "net", "sync", "io-util"] }

[workspace]
members = ["."]